@using DoneIt.DailyLogs.Data

<div class="daily-log-groups">
    @foreach (LogGroupEntry logGroupEntry in DailyLogEntry.DailyLogGroups)
    {
        <LogGroupLayout TestDesign="@TestDesign" LogGroupEntry="@logGroupEntry" />
    }
</div>
